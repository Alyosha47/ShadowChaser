# ShadowChaser — Master TODO (consolidated 2026-06-02)

Working view that pulls together everything outstanding from BACKLOG, HANDOFF, and
the new tweaks list. BACKLOG remains the durable source of detail; this is the
"what's on the table" overview for planning. IDs (#R, #F) cross-reference BACKLOG.
Tag [NEW] = from the latest tweaks list, not yet in BACKLOG.

---

## 0. DO FIRST — bank the working state
- [ ] Commit + push current files (BUILD `2026-06-02i`): online map correct, offline
      works when started offline, axis-only globe, minimal/standalone banner.
- [ ] Re-verify on the PHONE (`i`): online loads with labels; offline-from-start works.

---

## A. OPEN BUGS
- [ ] **#R4 live online↔offline switching** — map picks online/offline ONCE at launch;
      no live mid-session switch (either direction). Backed out this session (it kept
      destabilizing the working map). Own branch, NOT blind — needs device-console or a
      desktop repro; guardrail so it can never take down the working map.
- [ ] **Connectivity probe** — superseded for now (we use `navigator.onLine` at launch,
      dropped the Brave-blocked gstatic ping). Revisit only as part of #R4.
- [ ] **#R3 polar "onion-ring" fill** (deck.gl SolidPolygonLayer) — corridor/oval fills
      with polar vertices render phantom rings (e.g. 1950-09-12). 4 candidate fixes in
      BACKLOG. Hard; user said leave as-is for now.
- [ ] **#R5 iOS pinch-zoom not blocked** — `touch-action: pan-y` on scroll panels, leave
      the map container alone. Overlaps the "immobile/unscalable" tweak below. Test on real iPhone.
- [ ] **Safari geolocation fails; installed app works** [NEW] — locate-pin works in PWA,
      not Safari. Check secure-context / permissions / Brave default block vs code path.
      (Related to BACKLOG locate-pin note.)
- [ ] **Suggested/empty search text doesn't expand the search box** [NEW] — small CSS/JS.
- [ ] **Locate-pin feedback** — `setStatus('Locating…')` writes to the Search tab, so no
      visible feedback when on the Map tab. Needs map-context feedback.
- [ ] **Slow first load from local-disk server** — minutes vs seconds; profile the chunk-fetch.
- [ ] **Date label hard to see on map (esp. mobile)** — placement/contrast. Overlaps the
      mobile date-bar tweak below.
- [ ] **Scan ignores non-location filters** — always scans all 5 centuries. (Harmless
      offline now since all besselian is cached; the inefficiency remains.)
- [ ] **GE dot skew (VERIFY)** — greatest-eclipse dot offset; confirm still present after
      marker work; remove note if gone.

## B. MOBILE UX / LAYOUT PASS (interdependent — one sitting)
- [ ] **Banner + tabs permanent, immobile, unscalable** on mobile/PWA [NEW] — keystone;
      pairs with #R5 pinch-zoom. Don't scroll away or zoom.
- [ ] **Move tabs to screen BOTTOM** on mobile/PWA for thumb reach [NEW].
- [ ] **Single-line date/duration bar pinned at bottom** on mobile [NEW] — overlaps
      "date label hard to see" + "mobile map-click microsheet".
- [ ] **Map-tab mobile-vs-desktop disambiguation** [NEW] — desktop: map is ever-present,
      tab is a placeholder for future features; mobile: the tab IS the map. Different
      purpose → different look/behaviour.
- [ ] **Mobile map-click microsheet** — small dismissable bottom sheet showing at least
      umbral duration for the clicked point (no sidebar on mobile).
- [ ] (Done this session: banner minimal in browser, hidden in PWA — re-verify it
      survives the above.)

## C. OPEN UX DECISIONS (decide before coding)
- [ ] **Obscuration as local-only / #F5 global-vs-local search semantics** [NEW framing] —
      total = total globally; +location = total AS SEEN there; +obscuration = partial
      phase of a total at that location. Decide the model first.
- [ ] **Coordinates → "Location" rename + merge** — canonical term across details panel,
      popup, share text, parser comments; parser needs bracketed multi-word city support.
- [ ] **Instructions vs Search-Syntax sections** — merge into one?
- [ ] **Circumstances panel density** — Global Circs is tall; on map-click user should see
      local circs change without scrolling. Tighten or switch Global to compact list-table.
- [ ] **GE-dot zoom behavior** — cap max size in pixel space, not world space.

## D. FEATURES
### Easy
- [ ] Banner size: distinguish web vs app mode (partly done; verify).
- [ ] Map date more visible on mobile; eclipse-date overlay in desktop; hide redundant
      desktop map-status/date overlays.
- [ ] Dropped pin as a real 3-D-ish icon with shadow (proper MapLibre symbol-layer; emoji
      + SVG teardrop failed before).
### Medium
- [ ] Server-side share page (`/share?e=XXXXX`) — static HTML + map image; past the
      plaintext ceiling of navigator.share/mailto. Home for "prettier share" polish.
- [ ] Editable lat/lon/alt in the local-circumstances panel.
- [ ] Splash / title page for installed-PWA mode; final app icon / logo (provisional
      eclipse glyph in place now).
- [ ] Parabolic sun-track diagram in Details panel.
- [ ] Night-sky-during-totality view (planets/comets/bright stars near the Sun).
### Hard
- [ ] **#F2 Cloud-cover / weather overlay** — THE killer feature. Forecast + climatology;
      data source, globe-layer render, online/offline, controls, perf. ~2 sessions of
      design before code. USER's call.
- [ ] **#F3 Animated shadow on globe with time slider** — scrub umbra/penumbra. Most on-brand.
- [ ] **#F4 Topographic shadow overlay** — terrain shadows at observer location.
- [ ] **#F1 Personal "ShadowChaser log"** — visited / wishlist; schema, localStorage, UI.
- [ ] Thumbnail path map per list row (small SVG).
- [ ] Century scroller on mobile right edge.
- [ ] KMZ download.

## E. PERFORMANCE / DATA
- [ ] Frontload-cache a hot range (1900–2100) for instant common selections. (Partly
      realized via the service worker's field-data precache.)
- [ ] Path thumbnails feasibility/size for list rows.
- [ ] Drop / make-optional pre-1000 CE eclipses — load/data cost-benefit.
- [ ] Trim unused Cormorant Garamond weights (~70% font-payload cut).

## F. INFRA (offline keystones)
- [ ] Production bundling (single JS/CSS) — optimization now, not a blocker.
- [ ] Offline city labels — bundle Noto Sans PBF glyphs (~2–3MB) or alternative; offline
      is dots-only today.
- [ ] **Pro "download everything for the field" toggle** — Settings option to precache the
      full paths set (~274MB). Build only if a real user asks.

## G. REFACTOR LEDGER — Pass C (when a feature/bug motivates it)
- [ ] Init-time preconditions: wire selection before subscribers / buffer events until init.
- [ ] Search input still DOM-driven (not on AppState).
- [ ] `map.js` large/single-file split (deferred until a bug motivates).
- [ ] Event wiring for list/details; `AppState.on()` has no subscribers — wire only on demand.
- [ ] **Connectivity module** — promote when #R4 makes it a real second consumer.

## H. REVISIT AFTER LIVING WITH IT
- [ ] Umbra ovals: blink-off vs fade at zoom ≥ 7 (one-number threshold; feel preference).

---

## GROUND RULES (earned the hard way this session)
- Don't break what works. Stable > clever.
- No blind iOS fixes — observe (device console / repro) before coding connectivity.
- One coherent change at a time; bump BUILD; verify before stacking the next.
- Suggested re-entry: §0 bank state → one small contained win (search-box expand OR
  immobile banner/tabs) → mobile layout pass (§B) → bugs (§A) → decisions (§C) →
  features. #R4 only on its own guarded branch.
